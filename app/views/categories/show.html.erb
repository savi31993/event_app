<p id="notice"><%= notice %></p>
<h1><%= @category.name %></h1>
<div class="udesign">
  <div class="ui container">
    <div class="ui grid">
      <div class="sixteen wide column">
        <div class="ui three stackable cards">
          <% @category.events.each do |event| %>
            <div class="card">
              <div class="ui left piled segment">
                <img src="/image/image8.jpg" class="ui tiny left floated image">
                <h3 class="ui header text-center" style="margin-top: 0px;" ><%= link_to event.name, event %></h3>
                <div class="floated right text-center description">
                  <%= event.description %>
                </div>
                <% if current_user %>
                  <div class="row">
                    <div class="col-xs-12 col-md-6">
                      <a class="btn btn-success" href="#">Going</a>
                    </div>
                    <div class="col-xs-12 col-md-6">
                      <a class="btn btn-danger" href="#">Not Going</a>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

<%= link_to 'Back', categories_path %>
